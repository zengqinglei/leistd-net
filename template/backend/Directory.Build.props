<Project>

  <!--
    LeistdFrameworkVersion：项目引用的 Leistd.* 框架包版本。
    唯一事实来源是仓库根 VERSION 文件；此处为字面值（生成项目需自包含），
    由发布流水线（.github/workflows/release.yml）从唯一源同步，勿手工改。
  -->
  <PropertyGroup>
    <LeistdFrameworkVersion>0.8.0</LeistdFrameworkVersion>
  </PropertyGroup>

</Project>
