# {ModuleName} - 测试计划

## 1. 测试范围

### 1.1 包含

- {包含测试范围}

### 1.2 不包含

- {不包含测试范围}

## 2. 单元测试

| 对象 | 场景 | 预期 |
| --- | --- | --- |
| `{ClassOrFunction}` | `{scenario}` | `{expected}` |

## 3. 集成测试

| 接口/流程 | 数据准备 | 预期 |
| --- | --- | --- |
| `{api-or-flow}` | `{data}` | `{expected}` |

## 4. E2E / 手工验证

| 场景 | 步骤 | 预期 | 证据 |
| --- | --- | --- | --- |
| `{scenario}` | `{steps}` | `{expected}` | `{screenshot/log}` |

## 5. 权限与安全测试

- 未登录访问。
- 无权限访问。
- 越权访问其他用户/租户资源。
- 敏感字段不出现在响应和日志中。

## 6. 回归测试

- {历史缺陷或关键路径}

## 7. 未覆盖风险

| 风险 | 原因 | 后续计划 |
| --- | --- | --- |
| `{risk}` | `{reason}` | `{plan}` |
