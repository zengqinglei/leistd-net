# Leistd.ObjectMapping.AutoMapper

> 维护状态：暂缓维护。

该 provider 仅保留给已有 AutoMapper 集成使用。框架模板默认使用 `Leistd.ObjectMapping.Mapster`，新的组件和模板实现应优先选择 Mapster，除非业务明确需要 AutoMapper。

如需恢复主动维护，需要先升级 AutoMapper 依赖版本，并重新执行包漏洞检查。
