<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <!-- .Core 是打包边界，不是类型归属：命名空间剥掉它（同 Volo.Abp.Core → Volo.Abp） -->
    <RootNamespace>Leistd</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
  </ItemGroup>
</Project>
