<!--
  组件文档模板规范（内部约定，不面向最终读者）

  目标读者：**人类开发者 + AI 编码助手** 协作。
  风格基准：参考 Volo.ABP 文档——问题导向开篇、场景化标题、安装/配置/使用分明。
  参考样板：docs/components/lock.md（已按本模板撰写，撰写时对照它）。

  设计原则：
    1. 问题导向——先讲"解决什么问题、典型场景"，再讲 API；不要一上来就罗列接口。
    2. 场景化标题——用「何时使用」「配置 Provider」「使用」等读者能对应到任务的标题，
       不要用「核心抽象」「能力实现」这类技术黑话做主标题。
    3. AI 友好——章节固定、关键事实用表格/签名块，便于人机检索。
    4. 简洁可信——只描述源码中真实存在的公共 API；DI 方法名、接口名、命名空间、常量必须与源码一致，禁止臆造。
    5. 可操作——「使用」给出可直接复制的业务类示例（注入→调用）。

  ============ 章节骨架（按此顺序，标题用 ## ） ============

  # <组件能力的概念化标题>
  （例：「分布式锁与本地锁」「统一 API 响应」「链路追踪」——不带目录名/包名后缀）

  开篇 1–2 段：这个组件解决什么问题、为什么需要它、典型应用场景。问题导向，像 ABP 那样。

  ## 何时使用
  用表格或要点说明「什么场景用 / 选哪个实现 / 何时只引用抽象」。多实现组件务必给选型指引。

  ## 安装
  ```bash
  dotnet add package Leistd.Xxx.Core
  dotnet add package Leistd.Xxx.<实现>
  ```
  注明哪个是抽象、哪个是实现；提示模板项目用 CPM 无需写版本号。

  ## 配置 Provider   （无需 DI 注册的纯抽象组件可省略，或并入「使用」）
  在 Program.cs 注册的真实 DI 扩展方法（方法名必须与源码一致），说明各注册绑定了哪些接口。

  ## 使用
  可直接复制的业务类示例：构造注入接口 → 调用。覆盖该组件最常用的 1–2 个用法。

  ## 接口参考
  用表格列公共接口/方法/特性：| 成员 | 说明 |。方法写关键语义（返回什么、何时 null/抛异常）。

  ## 实现行为   （多实现或有重要运行时语义时；否则可省略）
  每个实现一小节（### 包名）：线程安全、过期、轮询、降级、后台服务等**源码确有**的语义与常量。

  ## 配置项 / Options   （有 Options 类时；否则省略，并在注意事项说明"当前无配置项"）
  列可配置属性、默认值。

  ## 注意事项   （可选）
  易踩坑/必须知道：返回 null 约定、单进程限制、硬编码常量、与 Java 版对应等。源码无依据的不写。

  ## 相关
  - [组件总览](./README.md)
  - 关联组件的相对链接

  ============ 硬性要求 ============
  - 中文撰写，简洁，不复述实现细节、不抄整段源码。
  - 只写源码真实存在的公共 API；方法名/接口名/命名空间/常量与源码逐一核对。
  - 省略不适用的章节（如纯抽象组件无「配置 Provider」「实现行为」），但保留「何时使用」「使用」「相关」。
-->
