<!--
  组件文档模板规范（内部约定，不面向最终读者）

  目标读者：**人类开发者 + AI 编码助手** 协作。
  设计原则：
    1. AI 友好——结构固定、信息可机读：每节有稳定标题，关键事实用表格/签名块而非散文。
    2. 简洁——只写"用这个组件需要知道的"，不复述源码、不堆砌实现细节。
    3. 可操作——每个组件给出「最小可用示例」，让人/AI 复制即用。
    4. 可信——只描述源码中真实存在的公共 API；不臆造、不写未实现的能力。

  每个分组文档（docs/components/<分组>.md）按下面的骨架填写。
  实现类多个时（如 lock 的 Core/Memory/Redis），「能力实现」一节按实现分小节。
-->

# <分组中文名>（`<目录名>`）

> 一句话定位：这个分组解决什么问题。

## 包

| 包 | 角色 | 何时引用 |
|---|---|---|
| `Leistd.Xxx.Core` | 抽象/接口 | 总是 |
| `Leistd.Xxx.Redis` | Redis 实现 | 需分布式时 |

## 核心抽象

> 列出**公共接口/基类**及其契约。每个用一句话说清职责，关键方法用签名 + 一句说明。

```csharp
public interface IXxx
{
    Task<...> DoAsync(...);   // 做什么、返回什么、何种情况返回 null/抛异常
}
```

## 能力实现

> 每个实现包一小节：注册方式 + 行为要点（线程安全、过期、降级等源码中确有的语义）。

### `Leistd.Xxx.Redis`
- 注册：`services.AddXxxRedis(...)`（写出真实的 DI 扩展方法名）
- 要点：……

## 最小可用示例

> 从注册到调用的最短闭环，可直接复制。

```csharp
// 1. 注册（Program.cs / DependencyInjection）
services.AddXxx();

// 2. 使用
public class Foo(IXxx xxx)
{
    public async Task BarAsync() => await xxx.DoAsync(...);
}
```

## 依赖

> 本分组依赖的其它 Leistd 组件（用于理解引用链），一行列出即可。

- `Leistd.Core`

## 备注（可选）

> 仅写"容易踩坑/必须知道"的点：与 Java 版的对应、未实现的边界、配置项默认值等。源码无依据的不写。
